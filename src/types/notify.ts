export type Notify = {
  reply: boolean
  reblog: boolean
  favourite: boolean
  follow: boolean
  follow_request: boolean
  reaction: boolean
  status: boolean
  poll_vote: boolean
  poll_expired: boolean
}
